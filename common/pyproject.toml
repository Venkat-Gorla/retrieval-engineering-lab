[project]
name = "common"
version = "0.1.0"
description = "Add your description here"

authors = [
    { name = "Venkat Gorla", email = "test@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.46",
    "opensearch-py>=3.2.0",
]

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
