[project]
name = "06-vector-indexing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "common",
    "datasets",
]

[tool.uv.sources]
common = { path = "../../common", editable = true }
datasets = { path = "../../datasets", editable = true }
